{
  "name": "silid-museo-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd silid-museo && npm install && npm run build",
    "dev": "cd silid-museo && npm run dev"
  }
}
